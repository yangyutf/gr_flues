package com.gr.his.utils.comm.demo;

import org.junit.Test;
/**
 * 测试动态库
 */
public class HttpLib {

    @Test
    public void testLib() {
       /* JNative.setLoggingEnabled(true);
        try {
            JNative getUrl = new JNative("D:/hp/ECCodePay.dll", "NationEcTrans"); //创建 getUrl 方法的JNative对象
            getUrl.setRetVal(Type.STRING); //设置返回值类型为：String
            getUrl.setParameter(0, "127.0.0.1"); //按顺序设置方法需要的参数值
            getUrl.setParameter(1, 10087);
            getUrl.setParameter(2, 123);
            getUrl.invoke(); //调用方法
            System.out.println(getUrl.getRetVal()); //输出返回值
        } catch (IllegalAccessException | NativeException e) {
            e.printStackTrace();
        }*/

    }
}
